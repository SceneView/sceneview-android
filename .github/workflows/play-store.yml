name: Deploy Demo to Play Store

on:
  push:
    tags:
      - 'demo-v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
    inputs:
      track:
        description: 'Play Store track'
        required: true
        default: 'internal'
        type: choice
        options:
          - internal
          - alpha
          - beta
          - production

jobs:
  deploy:
    name: Build & Publish Demo APK
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Decode keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.DEMO_KEYSTORE_BASE64 }}
        run: echo "$KEYSTORE_BASE64" | base64 -d > samples/sceneview-demo/release.keystore

      - name: Build release AAB
        env:
          SCENEVIEW_DEMO_KEYSTORE_FILE: release.keystore
          SCENEVIEW_DEMO_KEYSTORE_PASSWORD: ${{ secrets.DEMO_KEYSTORE_PASSWORD }}
          SCENEVIEW_DEMO_KEY_ALIAS: ${{ secrets.DEMO_KEY_ALIAS }}
          SCENEVIEW_DEMO_KEY_PASSWORD: ${{ secrets.DEMO_KEY_PASSWORD }}
        run: ./gradlew :samples:sceneview-demo:bundleRelease

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: sceneview-demo-release
          path: samples/sceneview-demo/build/outputs/bundle/release/*.aab

      - name: Publish to Play Store
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_SERVICE_ACCOUNT_JSON }}
          packageName: io.github.sceneview.demo
          releaseFiles: samples/sceneview-demo/build/outputs/bundle/release/*.aab
          track: ${{ github.event.inputs.track || 'internal' }}
          status: completed
          whatsNewDirectory: samples/sceneview-demo/play/release-notes
