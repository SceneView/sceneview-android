# Release v3.6.2 Trigger

This file was created to trigger the release workflow via workflow_dispatch.
Delete this file after release is complete.

To complete the release:
1. Go to Actions > Release > Run workflow
2. Or: `git tag -a v3.6.2 -m 'Release v3.6.2' && git push origin v3.6.2`
